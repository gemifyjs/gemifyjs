require "<%= gem_name %>/version"

module <%= name_for_class %>
  module Rails
    if ::Rails.version < "3.1"
      require "<%= gem_name %>/railtie"
    else
      require "<%= gem_name %>/engine"
    end
  end
end